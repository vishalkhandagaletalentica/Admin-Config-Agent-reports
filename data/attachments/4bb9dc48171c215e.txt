The mistake is a type mismatch, as the expected value type is a string (valueType 2), so the correct input should be a comma-separated list of allowed column names, for example: "TASK_TYPE,PROJECT_NAME,TASK_DESCRIPTION".