The command "update Sort Order to 12345;" is invalid because the Sort Order expects a value from the allowed sortable list, not an integer. The expected type is a string that consists of one or more columns from the sortable list, separated by commas if multiple columns are selected. 

A correct example value for the Sort Order could be: "A-PROJECT_NAME,D-DELAY_IN_DAY_AND_HOUR". 

You can use the command: "update Sort Order to A-PROJECT_NAME,D-DELAY_IN_DAY_AND_HOUR;".