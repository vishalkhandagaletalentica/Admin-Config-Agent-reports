I couldn't apply the update because the system could not validate the multi-list format for that entry. Could you provide the exact display mappings for the filters (for example: "AUTHOR -> Portfolio, CATEGORY -> Product Code, SUBJECT -> Customer, ATTRIBUTE1 -> <label>, ...") so I can update them correctly?