I can't apply "12345". The entry expects a string representing column labels (it's a label-key multiselect). The value should be a string. Please provide the column labels (the visible names) in the desired order so I can map them to keys and apply the update.