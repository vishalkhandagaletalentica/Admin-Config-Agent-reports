The command is invalid because the value type for "Column Order" is a multi-list, which requires a list of allowed values rather than a single integer. Please provide a comma-separated list of valid options. 

For example, a correct command could be: `update configuration set Column Order to "Option1,Option2,Option3"`.